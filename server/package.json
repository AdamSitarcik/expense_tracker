{
    "name": "server",
    "version": "1.0.0",
    "description": "Backend server for expense tracker app",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "nodemon server",
        "build": "rimraf ./build && tsc",
        "start": "npm run build && node build/index.js"
    },
    "author": "Adam Sitarcik",
    "license": "ISC",
    "devDependencies": {
        "@types/bcrypt": "^5.0.2",
        "@types/node": "^20.10.5",
        "nodemon": "^3.0.2",
        "rimraf": "^5.0.5",
        "ts-node": "^10.9.2",
        "typescript": "^5.3.3"
    },
    "dependencies": {
        "@prisma/client": "^5.7.1",
        "@types/express": "^4.17.21",
        "bcrypt": "^5.1.1",
        "express": "^4.18.2",
        "prisma": "^5.7.1"
    }
}
